#ifndef _SEARCH_H_
#define _SEARCH_H_

#include <stdio.h>
#include <stdlib.h>

#define NOT_FOUND -1

int advanced_binary(int *array, size_t size, int value);
void print_array(int *array, size_t ini, size_t end);

#endif
